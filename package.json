{
  "name": "snip-bundle",
  "version": "1.0.0",
  "description": "Snip — single-process bundle (backend + SPA + CLI)",
  "scripts": {
    "start": "bun server.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
